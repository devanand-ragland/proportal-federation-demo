import React from "react";
import Camera from "./pages/Unicorn/Camera";

//generate a functional component

const layout = () => {
    return (
        <div>
            <Camera />
        </div>
    );
    };

export default layout;

